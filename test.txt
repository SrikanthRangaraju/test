#!/bin/bash

# Load configuration
USER_NAME=""
PERIOD="GENERAL"
MONITORING_DATE="20221310"
SEGMENTS='"

# Define the gRPC function
process_line() {
  local line="$1"
  local rut=$(echo "$line" | cut -d',' -f1)
  local dv=$(echo "$line" | cut -d',' -f2)

  grpcurl -emit-defaults -d "{
 

# Process the input file
while IFS= read -r line; do
  echo "Processing line: $line"
  process_line "$line" >> output.log 2>> error.log &
done < testData.txt

# Wait for background processes to finish
wait
