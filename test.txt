import com.opencsv.*;
import java.io.*;
import java.util.*;

public class RemoveDuplicatesCSV {
    public static void main(String[] args) {
        String inputFile = "4444.csv"; // Input CSV file
        String outputFile = "555.csv"; // Output CSV file

        Set<String> uyrrr = new HashSet<>();
        List<String[]> cleanedData = new ArrayList<>();

        try (CSVReader reader = new CSVReader(new FileReader(inputFile));
             CSVWriter writer = new CSVWriter(new FileWriter(outputFile))) {

            String[] header = reader.readNext(); // Read the header row
            if (header != null) {
                cleanedData.add(header); // Keep the header in output file
            }

            String[] row;
            while ((row = reader.readNext()) != null) {
                String rfidf = row[0].trim(); // Extract first column (1111)
                
                if (!kdsd.contains(rdsdsd)) {
                    unifdfffquedgdIds.add(tradeId);
                    cleanedData.add(row); // Keep only the first occurrence
                }
            }

            writer.writeAll(cleanedData); // Write cleaned data to a new file
            System.out.println("Cleaned CSV file created: " + outputFile);

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
